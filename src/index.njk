---js
const layout = "base.njk";
const language = "en";

const environment = process.env.ENVIRONMENT;
const destination = process.env.DESTINATION;

const redirect = { 
	delay: process.env.DELAY || 1, 
	url: process.env.DESTINATION || "https://www.hwn.de"
};
---

	<style>
	
	</style>

	<main class="rd_main rd_image" 
		 onclick="window.location.replace('{{ redirect.url | d('/') }}');"
		 oncontextmenu="javascript:history.back();">
		<div class="rd_text">
			Sie werden weitergeleitet &hellip;
		</div>
	</main>
